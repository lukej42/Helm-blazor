myapp ({{ .Values.environment }}) has been deployed.

Release:    {{ .Release.Name }}
Namespace:  {{ .Release.Namespace }}
Replicas:   {{ .Values.replicaCount }}
Image:      {{ .Values.image.repository }}:{{ .Values.image.tag }}
Service:    {{ .Values.service.type }}

1. Watch the LoadBalancer external IP:
   kubectl get svc {{ include "myapp.fullname" . }} -n {{ .Release.Namespace }} -w

2. Confirm pods are scheduled on different nodes:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -o wide

{{- if .Values.topologySpreadConstraints.enabled }}
Topology spread is enabled (maxSkew={{ .Values.topologySpreadConstraints.maxSkew }}, topologyKey={{ .Values.topologySpreadConstraints.topologyKey }}, whenUnsatisfiable={{ .Values.topologySpreadConstraints.whenUnsatisfiable }}, minDomains={{ .Values.topologySpreadConstraints.minDomains }}).
The cluster must have at least two Ready nodes for both replicas to schedule when whenUnsatisfiable is DoNotSchedule.
{{- end }}
